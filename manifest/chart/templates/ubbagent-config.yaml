{{- if .Values.billing.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-ubbagent" (include "glassbox-ipfto-screening.fullname" .) | quote }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "glassbox-ipfto-screening.labels" . | nindent 4 }}
data:
  config.yaml: |
    identities:
      - name: gcp
        gcp:
          encodedServiceAccountKey: ${MARKETPLACE_REPORTING_KEY_B64}
    metrics:
      - name: {{ .Values.billing.ubbagent.metricNameStandalone | quote }}
        type: int
        endpoints:
          - name: servicecontrol
      - name: {{ .Values.billing.ubbagent.metricNameIntegrated | quote }}
        type: int
        endpoints:
          - name: servicecontrol
    endpoints:
      - name: servicecontrol
        servicecontrol:
          identity: gcp
          serviceName: {{ .Values.billing.ubbagent.serviceName | quote }}
          consumerId: ${MARKETPLACE_CONSUMER_ID}
{{- end }}
